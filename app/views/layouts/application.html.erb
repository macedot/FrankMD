<!DOCTYPE html>
<html lang="<%= I18n.locale %>" class="h-full">
  <head>
    <title><%= content_for(:title) || "FrankMD" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="FrankMD">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>

    <%= yield :head %>

    <link rel="icon" type="image/svg+xml" href="/icon.svg">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">

    <%= stylesheet_link_tag "fonts" %>

    <% if Rails.application.assets.load_path.find("tailwind.css") %>
      <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <% elsif Rails.application.assets.load_path.find("tailwind/application.css") %>
      <%= stylesheet_link_tag "tailwind/application", "data-turbo-track": "reload" %>
    <% end %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-white dark:bg-zinc-900 text-zinc-900 dark:text-zinc-100 font-sans antialiased">
    <!-- Inline loading screen - shows instantly before CSS/JS loads, hidden by app_controller -->
    <div id="app-loading" style="position:fixed;inset:0;z-index:9999;display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,#2D1B4E 0%,#FF6B9D 100%)">
      <div style="text-align:center;color:white;font-family:system-ui,sans-serif">
        <img src="/icon.svg" alt="FrankMD" style="width:150px;height:150px;margin-bottom:1.5rem;filter:drop-shadow(0 4px 20px rgba(0,0,0,0.3))">
        <div style="width:36px;height:36px;margin:0 auto;border:3px solid rgba(255,255,255,0.3);border-top-color:white;border-radius:50%;animation:spin 1s linear infinite"></div>
        <style>@keyframes spin{to{transform:rotate(360deg)}}</style>
      </div>
    </div>
    <%= yield %>
  </body>
</html>
